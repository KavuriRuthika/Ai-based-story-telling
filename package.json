{
  "name": "kathaquest",
  "version": "1.0.0",
  "description": "AI-based adaptive storytelling platform for children",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "dev:verbose": "concurrently --names Backend,Frontend \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && python -m uvicorn app.main:app --reload",
    "frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && python -m venv venv && cd ../frontend && npm install",
    "build": "concurrently \"npm run build:backend\" \"npm run build:frontend\"",
    "build:backend": "echo Backend is ready (no build needed)",
    "build:frontend": "cd frontend && npm run build",
    "setup": "node scripts/setup.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rxjs": "^7.8.2"
  }
}
